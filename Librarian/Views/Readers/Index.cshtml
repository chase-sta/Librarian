@model IEnumerable<Librarian.Models.Reader>

<table class="table">
    <tr class="table-secondary">
        <th>Name</th>
        <th>Age</th>
        <th>Email</th>
        <th></th>
    </tr>
    @*@foreach (var reader in Model)
    {
    <tr>
    <td> @reader.Name</td>
    <td> @reader.Age</td>
    <td> @reader.Email</td>
    <td>
    <a class="btn btn-warning"> Edit </a>
    <a class="btn btn-danger"> Delete </a>
    </td>
    </tr>
    }*@
</table>